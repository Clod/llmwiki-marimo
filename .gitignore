# Dependencies
node_modules/
.venv/
venv/

# Demo wikis materialized by quickstart.py (the tracked sources live in examples/)
/wikis/

# MCP config (contains user IDs)
.mcp.json

# Build
.next/
dist/
*.egg-info/

# Secrets — NEVER commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc

# IDE
.idea/
.vscode/
# ...except the shared markdown guard: without it, editors reformat .md on
# save and mangle emphasis/links. Personal editor prefs stay in User settings.
!.vscode/
!.vscode/settings.json
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# LLM Wiki local workspace artifacts
.llmwiki/
workspace/
/wiki/
/mcp/wiki/
/emails/

# Private launch / marketing drafts — never publish
launch-kit.md

# Scratch prototypes + inspiration notes — kept locally, out of the public repo
marimo/prototypes/
Karpathy_concepts.md

# Internal release-readiness review (candid; not for the public repo)
docs/release_readiness_review.md

# Deploy
deploy.sh

# Local Netlify folder
.netlify
nvidia-10k-first20.md

.claude
!.claude/commands/
.playwright-mcp
.pytest_cache

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
tests/fixtures/workspace/
tests/fixtures/workspace_e2e/

# Generated reports (codemap scans, etc.)
.reports/

# Golden-corpus staging workspace (scratch; the frozen copy lives in golden_corpus/)
tests/fixtures/_golden_staging/

# Generated eval packets (model-quality scorecards; regenerate via build_eval_packet.py)
eval_reports/

# SQLite transient sidecars — never commit (the tracked golden_corpus/index.db is the fixture)
*.db-wal
*.db-shm
*.db-journal

# Understand-Anything knowledge graph (regenerable)
.ua/
